#!/bin/zsh
__wrap_notice() {
	if [[ "$ZSH_DEBUG" != "0" ]]; then
		local name="$1"
		local path="$(command -v "$name" 2>/dev/null || true)"
		if [[ -n "$path" ]]; then
		    print -P  "${dim}[wrap] ${name} -> ${path}${reset}"
		fi
	fi
}
